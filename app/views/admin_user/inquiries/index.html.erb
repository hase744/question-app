<%= render "/admin_user/shared/index", 
  	models: @inquiries, 
  	column_names: ['id', 'is_replied', 'body', 'name', 'body'], 
    show_exist: true,
    delete_exist: false,
    edit_exist: true,
    links: -> (model){ []}
  	%>
