<div class="request_like_show">
    <h3 class="input_new_title"><%= link_to "マイページ", user_configs_path, class:"main_area_top_link"%> ＞ いいねした質問 <%= paginate @requests %></h3>
  	<% @requests.each do |request| %>
		  <%= render "user/requests/cell", request: request %>
  	<% end %>
  	<%= paginate @requests %>
</div>
<script>

var swiper = new Swiper('.swiper-container', {
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },
});
</script>