<span class="category_link_area">
  	<div class="category_select_button">
  	  	<%= link_to category[:japanese_name], user_transactions_path(category_names: category[:name]), class:"category_link"%>
  	</div>
  	<% category[:child_categories].each do |category| %>
  	  	<div class="space"></div><%= render "user/homes/category_link", category:category %>
  	<% end %>
</span>