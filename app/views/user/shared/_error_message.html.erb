<% if model && model.errors.any? %>
  <div class="error_component_area">
    <% model.errors.each do |attribute, message| %>
      <div class="alert alert-danger">
        <i class="fas fa-exclamation-triangle"></i> <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
